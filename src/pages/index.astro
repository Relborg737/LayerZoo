---
---
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>LayerZoo • Bitcoin Honey Badger Genesis</title>
  <meta name="description" content="2100 free Bitcoin Honey Badger NFTs on Stacks. Promoting Bitcoin & L2s." />
  <link rel="icon" href="/favicon.svg" />
</head>
<body class="bg-black text-white min-h-screen">
  <main class="max-w-4xl mx-auto px-6 py-16 text-center">
    <h1 class="text-8xl font-display text-orange font-black mb-4">LAYERZOO</h1>
    <p class="text-xl text-gray-400 mb-12">Bitcoin Honey Badger Genesis • 2100 free NFTs on Stacks</p>

    <img src="/badger-hero.png" alt="Honey Badger" class="mx-auto w-full max-w-xl rounded-2xl border-4 border-orange shadow-2xl mb-12" />

    <h2 class="text-5xl font-display text-orange mb-8">2100 BADGERS • FREE • EARNED ONLY</h2>
    <p class="text-2xl leading-relaxed mb-12">
      Honey Badger don't care.<br />
      2100 Genesis dropping soon. No presale. No price.<br />
      Just proof of contribution to Bitcoin & its Layer 2s.
    </p>

    <div id="countdown" class="grid grid-cols-4 gap-6 max-w-2xl mx-auto mb-12 text-4xl"></div>

    <div class="space-x-6">
      <a href="https://t.me/BitcoinL2Crew" target="_blank" class="inline-block bg-orange text-black px-10 py-5 rounded-xl font-bold text-xl hover:bg-orange/90 transition">
        Join the Pack
      </a>
      <a href="#" class="inline-block border-2 border-orange text-orange px-10 py-5 rounded-xl font-bold text-xl hover:bg-orange hover:text-black transition">
        Allowlist Form →
      </a>
    </div>

    <footer class="mt-24 text-gray-500">
      Open-source • <a href="https://github.com/Relborg737/LayerZoo" target="_blank" class="text-orange underline">GitHub</a>
    </footer>
  </main>

  <script>
    const drop = new Date("2025-04-01T00:00:00").getTime();
    const countdown = document.getElementById("countdown");
    const timer = setInterval(() => {
      const distance = drop - Date.now();
      if (distance < 0) { 
        countdown.innerHTML = "<div class='col-span-4 text-5xl text-orange'>LIVE</div>"; 
        clearInterval(timer); 
        return; 
      }
      const days = Math.floor(distance / 86400000).toString().padStart(2, '0');
      const hours = Math.floor((distance % 86400000) / 3600000).toString().padStart(2, '0');
      const mins = Math.floor((distance % 3600000) / 60000).toString().padStart(2, '0');
      const secs = Math.floor((distance % 60000) / 1000).toString().padStart(2, '0');
      countdown.innerHTML = `
        <div class="bg-gray-900 rounded-xl py-6"><span class="block text-6xl">${days}</span>Days</div>
        <div class="bg-gray-900 rounded-xl py-6"><span class="block text-6xl">${hours}</span>Hours</div>
        <div class="bg-gray-900 rounded-xl py-6"><span class="block text-6xl">${mins}</span>Mins</div>
        <div class="bg-gray-900 rounded-xl py-6"><span class="block text-6xl">${secs}</span>Secs</div>
      `;
    }, 1000);
  </script>
</body>
</html>
